from setuptools import find_packages, setup

setup(
    name="medicare-ai",
    version="2.0.0",
    description="MediCare AI — a RAG-based medical information chatbot (Flask + LangChain + Pinecone + Groq)",
    author="Pritam",
    packages=find_packages(),
    install_requires=[],
)
